(function (app) {
    app.factory('personService', ['$http', 'config', "util", function ($http, config, util) {
        return {
            
        };
    }]);
})(angular.module('app.services'));