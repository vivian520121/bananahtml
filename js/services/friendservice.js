(function (app) {
    app.factory('friendService', ['$http', 'config', "util", function ($http, config, util) {
        return {
            
        };
    }]);
})(angular.module('app.services'));